package Cellar.View;

import Cellar.Model.Field;
import Cellar.Model.Room;
import javafx.scene.canvas.GraphicsContext;
import javafx.scene.paint.Color;

import static Cellar.Model.Model.cornerSize;

public class ShowRoom {
/*
    static public void showRoom(GraphicsContext gc, Room room){
        for(Field f: room.fields){
            gc.setFill(Color.GRAY);
            gc.fillRect((room.x+f.x)*cornerSize, (room.y+f.y)*cornerSize, cornerSize-1, cornerSize-1);
        }
    }*/
}
